if __name__ == "__main__":

    def test_func(**kwargs):
        pass
        # for item in range(*args):
        #     kwargs = {
        #         '{0}__{1}__icontains'.format(field_name, foreign_key_second_field): field_value
        #     }
        # for key,value in kwargs.items():
        #     lodo.update({
        #         f'{key}__':value,
        #     })
        # ''.join(f'{value}' for _ in range(20)) + str(current_time) + str(today)

    # print(test_func(name="ali",age=12))